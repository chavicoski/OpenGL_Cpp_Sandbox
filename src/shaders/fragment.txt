#version 330 core

in vec3 fragColor;
in vec2 texCoord;

out vec4 screenColor;

uniform sampler2D ourTexture;

void main() {
    screenColor = texture(ourTexture, texCoord) * vec4(fragColor, 1.0); ;
}